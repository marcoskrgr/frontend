const confirmPhoneInputs = {
    canSubmit: false,
    inputs: [
      { id: "code", label: "Preencha com o código enviado ao seu Telefone!", inputMode: "numeric", isRequired: true, alignCenter: true, type: "number", error: "", value: "" }
    ]
  };
  
export default confirmPhoneInputs;
